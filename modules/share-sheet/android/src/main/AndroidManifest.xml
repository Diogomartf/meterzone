<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <provider
      android:name="expo.modules.sharesheet.ShareSheetFileProvider"
      android:authorities="${applicationId}.ShareSheetFileProvider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/share_sheet_provider_paths" />
    </provider>
  </application>
</manifest>
